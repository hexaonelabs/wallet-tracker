import { CalculPercentPipe } from './calcul-percent.pipe';

describe('CalculPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
