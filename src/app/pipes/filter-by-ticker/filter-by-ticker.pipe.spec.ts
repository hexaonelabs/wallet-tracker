import { FilterByTickerPipe } from './filter-by-ticker.pipe';

describe('FilterByTickerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByTickerPipe();
    expect(pipe).toBeTruthy();
  });
});
