import { TotalPercentPipe } from './total-percent.pipe';

describe('TotalPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
