import { InitialInvestPipe } from './initial-invest.pipe';

describe('InitialInvestPipe', () => {
  it('create an instance', () => {
    const pipe = new InitialInvestPipe();
    expect(pipe).toBeTruthy();
  });
});
