import { TotalPositionPipe } from './total-position.pipe';

describe('TotalPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
